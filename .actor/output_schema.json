{
  "actorOutputSchemaVersion": 1,
  "title": "PagineGialle Scraper Output",
  "description": "Italian business listings extracted from PagineGialle.it",
  "properties": {
    "results": {
      "title": "Business Listings",
      "type": "string",
      "description": "Full dataset of extracted Italian business listings",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=json"
    },
    "resultsCSV": {
      "title": "Download CSV",
      "type": "string",
      "description": "Download all results as CSV",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=csv"
    },
    "resultsXLSX": {
      "title": "Download Excel",
      "type": "string",
      "description": "Download all results as Excel",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=xlsx"
    }
  }
}
