{
  "title": "PagineGialle Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searches": {
      "title": "Search Queries",
      "type": "array",
      "description": "List of searches. Each needs 'what' (category) and 'where' (city).",
      "editor": "json",
      "default": [
        {
          "what": "ristoranti",
          "where": "napoli"
        }
      ],
      "prefill": [
        {
          "what": "ristoranti",
          "where": "napoli"
        }
      ]
    },
    "maxResults": {
      "title": "Max Results per Search",
      "type": "integer",
      "description": "Maximum listings per search query. Large cities (Roma/Milano) can yield 1000+.",
      "default": 200,
      "minimum": 1,
      "maximum": 5000
    },
    "onlyWithPhone": {
      "title": "Only listings with phone",
      "type": "boolean",
      "default": false,
      "description": "Filter out listings without a phone number."
    },
    "onlyWithWebsite": {
      "title": "Only listings with website",
      "type": "boolean",
      "default": false,
      "description": "Filter out listings without a website."
    },
    "onlyWithEmail": {
      "title": "Only listings with email",
      "type": "boolean",
      "default": false,
      "description": "Filter out listings without an email address."
    },
    "useApifyProxy": {
      "title": "Use Apify Proxy (recommended)",
      "type": "boolean",
      "description": "Use Apify residential proxies for IP rotation. Strongly recommended for multi-search runs.",
      "default": true
    },
    "proxyCountry": {
      "title": "Proxy Country",
      "type": "string",
      "default": "IT",
      "enum": [
        "IT",
        "DE",
        "FR",
        "ES",
        "GB",
        "US"
      ],
      "enumTitles": [
        "Italy",
        "Germany",
        "France",
        "Spain",
        "UK",
        "US"
      ],
      "description": "Country code for residential proxy. IT recommended for Italian sites."
    }
  },
  "required": [
    "searches"
  ]
}